"use client";

import { Text, View } from "react-native";

/** Placeholder until US3 (`tasks.md` T046). */
export default function ListingsPlaceholderPage() {
  return (
    <View className="flex-1 bg-rose-pine-base p-4">
      <Text className="text-rose-pine-muted">
        Listings will be available in the next milestone.
      </Text>
    </View>
  );
}
