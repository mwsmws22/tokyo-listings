---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# tokyo-listings Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-28

## Active Technologies

- TypeScript (strict), Node.js 22 LTS for Next.js; Bun 1.x for package manager + Next.js (App Router), Tamagui, tRPC, TanStack Query, Jotai, Hono, (001-tokyo-listings-baseline)

## Project Structure

```text
apps/web/
apps/api/
packages/db/
tests/
```

## Commands

bun run test && bun run lint

## Code Style

TypeScript (strict), Node.js 22 LTS for Next.js; Bun 1.x for package manager: Follow standard conventions

## Recent Changes

- 001-tokyo-listings-baseline: Added TypeScript (strict), Node.js 22 LTS for Next.js; Bun 1.x for package manager + Next.js (App Router), Tamagui, tRPC, TanStack Query, Jotai, Hono,

<!-- MANUAL ADDITIONS START -->

## Development environment (SSH)

This repo is developed **over SSH**: Cursor (IDE and agent) runs in **Remote-SSH** mode against the user’s server, with the workspace and terminal on that machine—not on a local macOS/Windows box.

When running commands, testing, or reasoning about paths and tooling, **assume Linux** (shell, package managers, file paths, process model). Prefer standard POSIX/Linux conventions unless the project explicitly documents otherwise.

## Dependency installs (agents)

Do **not** run `bun install` (or other package-manager installs) from automated agent sessions. If dependencies are missing, the lockfile changed, or installs need to run after permission fixes, **ask the repository owner** to run this at the repo root:

`bun install`

Then continue with lint, typecheck, or other tasks.

## Local dev ports

- **Web** (Next.js): `3000` by default.
- **API** (Hono): **`4001`** by default (`PORT` in `apps/api`, `API_DEV_ORIGIN` / Next rewrites default to `http://localhost:4001`). Do not use `8787` for this project.

<!-- MANUAL ADDITIONS END -->
